webdriver {
  driver = chrome
  base.url = "http://localhost:5173"
}

headless.mode = false

chrome.switches = "--no-sandbox;--disable-gpu;--window-size=1920,1080;--disable-dev-shm-usage"

serenity {
  project.name = "FoodTech E2E Automation"
  take.screenshots = AFTER_EACH_STEP
}

test {
  credentials {
    valid {
      email = "demo@foodtech.com"
      password = "kv20012001"
    }
    invalid {
      email = "wrong@foodtech.com"
      password = "wrongpass"
    }
  }
}
